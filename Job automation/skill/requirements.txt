# Core pipeline runs on the Python stdlib + these two.
PyYAML>=6.0
# httpx is only needed for the direct-fetch backend (OpenClaw on an unblocked host).
httpx>=0.27

# JobSpy = open-source, no-key discovery backbone (LinkedIn/Indeed/Google/Glassdoor).
# Replaces the paid JSearch/RapidAPI route. Pulls in pandas.
python-jobspy>=1.1.76

# Optional: PDF export (cli pdf / render). Renders the print-styled HTML to A4 PDF.
weasyprint>=60
# Optional: nicer Markdown->HTML for editable application packets (a small built-in
# fallback parser is used if this is not installed).
markdown>=3.5
